面试题：
- common 包（配置文件(处理常用的常量或者静态数据的处理, 配置文件需要手功的去维护, 维护少量的数据让自动框架有效的运行)、
            数据库处理类(数据库的校验, 测试用例和数据库进行关联)、
            Excel表格操作处理类(管理测试用例, 和ddt搭配进行数据的管理)、
            logger日志操作、(出现问题查看日志处理器)
            Requests类(处理接口HTTP请求库, 访问接口)）
- 测试数据放到 data 包
- 配置文件 config 包
- 测试报告 report
- 测试用例方法 cases
- run.py 收集测试用例

# 断言的方式
- 全量断言
- 部分断言

修改 ddt 源码，修改的源码存放在libs文件夹

json 和 python 类型转换：
Json 标准格式的 1、None会转换成null ,2、引号会转化成双引号
Json.dumps(字典), Json.loads(字符串)

不符合格式：报错：
try：
    pass
except:
    pass


运行时候的坑：
坑1、在写完模块里面的代码(类、函数) 一定要在末尾空一行。
运行测试用例：类名上运行，不要再测试用例方法上去运行，否则会报错滴

# 注册功能
# 注册成功手机号码怎么判断是不是在现有系统里存在
# 注册手机号码一定要生成数据：(辅助函数)，密码.
# 生成手机号码：gen_phone , 动态生成

# 投资用例，可以动态生成投资金额 gen_money
# 手机号码 查询数据库，设计到了while循环，判断手机号

# 充值：前置条件：登录
# 1、放到 setUP 里面
# 2、放到第一条测试用例里面， 测试用例里面动态修改 header 数据


